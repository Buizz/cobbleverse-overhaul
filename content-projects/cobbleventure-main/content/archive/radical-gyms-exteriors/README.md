# Radical Gyms 외관 원본 보관소

이 디렉터리의 NBT 7개는 공용 `content/structures/gyms/base_gym.nbt`로 통합하기 전
가져온 Radical Gyms 원본이다. `content/structures` 밖에 있으므로 웹 NBT 목록,
건축 월드와 데이터 모드 빌드에는 포함되지 않는다.

실제 체육관 외관 수정은 이 원본이 아니라 `base_gym.nbt`에서 진행한다.
